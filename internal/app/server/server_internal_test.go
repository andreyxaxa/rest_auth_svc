package server

import (
	"testing"
)

func TestServer_HandleTest(t *testing.T) {
	// s := newServer() //тестовое хранилище

	// rec := httptest.NewRecorder()
	// req := httptest.NewRequest("GET", "/test", nil)

	// s.handleTest().ServeHTTP(rec, req)

	// assert.Equal(t, rec.Body.String(), "hello")
}
